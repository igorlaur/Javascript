const resultado = nota => nota >= 7 ? 'Aprovado' : 'Reprovado' // Se for verdadeiro, retorna Aprovado. Senão... Forma mais simples e reduzida sem if e else

console.log(resultado(7.1))
console.log(resultado(6.7))

